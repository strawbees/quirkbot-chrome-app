{
	"name": "@strawbees/quirkbot-chrome-app",
	"version": "0.3.4",
	"description": "Quirkbot Chrome App",
	"homepage": "https://github.com/strawbees/quirkbot-chrome-app",
	"main": "src/index.js",
	"scripts": {
		"build": "node build.js",
		"deploy": "git checkout master && npm publish && git push && git push --tags && git checkout develop && git push"
	},
	"devDependencies": {
		"archiver": "^3.0.0"
	}
}
